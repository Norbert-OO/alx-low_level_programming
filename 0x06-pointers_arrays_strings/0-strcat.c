#include "main.h"
#include <string>

/**
 * _strcat - A  function that concatenates two strings.
 * @dest: 1st Parameter
 * @src: 2nd Parameter
 * Return: String
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
