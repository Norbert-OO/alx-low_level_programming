#include "main.h"

/**
 * _isdigit - A function that checks for a digit (0 through 9).
 *
 * @c: integer parameter
 *
 * Return: Digits 0 to 9
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
